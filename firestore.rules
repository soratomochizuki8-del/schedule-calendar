rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // カレンダードキュメントへのアクセス
    match /calendars/{calendarId} {
      // 誰でも読み取り・書き込み可能（URLを知っている人だけが使える想定）
      allow read, write: if true;
    }
  }
}
